info:
  name: generate
  type: http
  seq: 1

http:
  method: POST
  url: "{{DEV_URL}}/otp/generate"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
