info:
  name: Get a user's profile info
  type: http
  seq: 1

http:
  method: GET
  url: "{{DEV_URL}}/user/info"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
